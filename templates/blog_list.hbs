{{# partial content }}
  {{#each this}}
    <a href="/blog/{{url}}" class="post-listing">
      <h1>{{title}}</h1>
      <h2 class="date">{{created}}</h2>
      <p>{{summary}}</p>
    </a>
    <!-- <hr /> -->
  {{/each}}
{{/partial}}

{{> body}}
